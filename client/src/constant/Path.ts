class Path {
	ROOT = 'localhost:3000'

	LOGIN = `/`
	REGISTER = `/register`
	
	MENU = `/menu`
	LIBRARY = `/library`
	ACCOUNT = '/account'
	FRIENDS = `/friends`
	SUPPORT = `/support`
	REQUEST = `/request`
	TICKETS = `/tickets`
}

export default new Path()